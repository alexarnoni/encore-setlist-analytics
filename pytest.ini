[pytest]
pythonpath = src
testpaths = tests
; tests/integration/ needs a real Postgres (postgres-test) and is
; excluded from the default `pytest` run; invoke it explicitly with
; `pytest tests/integration` (see README.md "Integration tests").
norecursedirs = integration
